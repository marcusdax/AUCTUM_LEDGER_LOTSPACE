# openai@^4.77 declares peerOptional zod@^3.23.8 while the platform pins zod@^4.4.3
# (SDD ruling: keep zod 4.x, do NOT downgrade). zod is an optional peer used only by
# openai's zod-to-JSON-schema helpers, which the API proxy does not call.
legacy-peer-deps=true
